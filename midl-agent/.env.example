# MIDL Agent Configuration
#
# Get your API key:
# 1. Join the MIDL Discord: https://discord.com/invite/midl
# 2. Complete verification in the welcome channel
# 3. Run /setup-mcp in any channel

MCP_API_KEY=your-mcp-api-key-here

# ===========================================
# HTTP MCP Server Configuration (v1.1+)
# ===========================================

# Transport mode: "stdio" (default) or "http"
# MCP_TRANSPORT=http

# HTTP server settings (only used when MCP_TRANSPORT=http)
# MCP_SERVER_PORT=3847
# MCP_SERVER_HOST=0.0.0.0

# Allowed origins for CORS (comma-separated, empty = allow all for local dev)
# MCP_ALLOWED_ORIGINS=https://example.com,https://app.example.com

# Connection timeout in milliseconds (default: 30000)
# MCP_CONNECTION_TIMEOUT_MS=30000
